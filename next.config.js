/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // For GitHub Pages deployment (optional - uncomment if using GitHub Pages)
  // output: 'export',
  // basePath: process.env.NEXT_PUBLIC_BASE_PATH || '',
  // images: {
  //   unoptimized: true,
  // },
}

module.exports = nextConfig

